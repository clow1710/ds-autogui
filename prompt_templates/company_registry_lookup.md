你需要使用网页搜索，逐条核验下列公司在中国大陆登记信息中的"企业所在地址"以及"是否属于非公有制企业"。

批次 ID：{{BATCH_ID}}

待核验公司列表：

```json
{{COMPANIES_JSON}}
```

## 输出格式要求

- 最终回复必须是一个可直接被 `JSON.parse` 解析的 JSON 对象。
- 必须以**单行紧凑**形式输出：所有字段写在同一行，不得包含任何换行或缩进空白。
- 第一个字符必须是 `{`，最后一个字符必须是 `}`。
- 严禁使用 Markdown 代码块或围栏标记（不允许出现 ```、```json、```JSON 等）。
- 不要输出搜索过程、判断步骤、解释、结论摘要、前言或后记，也不要输出代码块语言名或复制/下载按钮文字。

## 搜索与判断规则

1. 每家公司独立搜索并输出一条结果，保持输入的 `id` 和 `name` 不变；`items` 中每个输入公司只能出现一次。
2. 信息来源优先级：
   - 首选官方来源：国家企业信用信息公示系统、地方市场监督管理局/政务服务平台、信用中国等政府公开登记信息。
   - 次选可信商业信息站点：企查查、天眼查、爱企查、启信宝、企查猫等，用于交叉核验。
   - 企业官网、政府/园区/行业主管部门页面等可信来源也可作为补充。
3. 不要编造或猜测缺失信息。**任何字段只要存在不确定（来源缺失、来源较弱、来源冲突且无法判断、仅能推断等），一律填 `null`。**

## 字段说明

### `registered_address`（字符串或 `null`）

企业所在地址。

- 优先使用登记字段："注册地址"、"住所"、"主要经营场所"、"经营场所"。
- 若找不到登记地址，可使用其他可信来源中的企业所在地址。
- **必须以省级行政单位（省、自治区、直辖市、特别行政区）的完整名称起始，一律不得省略**。例如必须写 `"河南省郑州市金水区..."`，不允许写成 `"郑州市金水区..."`；必须写 `"北京市朝阳区..."`，不允许写成 `"朝阳区..."`；必须写 `"内蒙古自治区呼和浩特市..."`，不允许写成 `"呼和浩特市..."`。
- 无法确认（或来源仅给出无省级前缀的地址、不能可靠地补齐省级单位）时填 `null`，不要使用空字符串。

### `is_non_public`（布尔值或 `null`）

表示企业是否属于**非公有制**企业。

- 当企业的登记类型属于以下任一时，填 `false`（即属于公有制，**不是**非公有制）：
  - 国有企业（含"全民所有制企业"）
  - 国有独资公司
  - 集体企业（含"集体所有制企业"）
  - 国有资本控股公司
  - 国有资本参股公司
- 其他登记类型一律填 `true`，包括但不限于：私营有限责任公司、私营股份有限公司、其他有限责任公司、其他股份有限公司、股份合作企业、联营企业、个人独资企业、合伙企业、个体工商户、农民专业合作社、港澳台投资企业、外商投资企业等。
- 无法确认企业类型时填 `null`，不要猜测。

## 输出模板

字段不能省略；未知值使用 `null`（不要使用空字符串）。实际输出请勿换行、勿使用代码块：

`{"batch_id":"{{BATCH_ID}}","items":[{"id":"输入中的 id","company_name":"输入中的 name","registered_address":"河南省郑州市金水区...","is_non_public":true}]}`
